/*al presionar el botón repetir hasta que utilizamos 'BREAK'.*/


function mostrar()
{	
	var numeroIngresado;
	var i = 0;
	numeroIngresado = prompt("ingrese un numero");
	numeroIngreado = parseInt(numeroIngresado);

	for( i = numeroIngresado ;i == numeroIngresado;i=numeroIngresado)
{
	numeroIngresado = prompt("ingrese un numero");
	numeroIngresado = parseInt(numeroIngresado);

	if(numeroIngresado==9)

	{
		break;
	}
}

}//FIN DE LA FUNCIÓN